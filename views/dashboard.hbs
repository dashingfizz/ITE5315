<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}}</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body class="bg-light">

    <!-- Navbar -->
    <nav class="navbar navbar-expand-lg navbar-dark custom-navbar">
        <div class="container">
            <a class="navbar-brand" href="/">
                🍔 GameDay Eats
            </a>

            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#mainNav">
                <span class="navbar-toggler-icon"></span>
            </button>

            <div class="collapse navbar-collapse" id="mainNav">
                <div class="navbar-nav ms-auto">
                    <a class="nav-link" href="/">Home</a>

                    {{#if user}}
                        <span class="nav-link disabled d-none d-lg-inline">Welcome, {{user.email}}</span>
                        <a class="nav-link active" href="/dashboard">Dashboard</a>
                        <a class="nav-link" href="/auth/logout">Logout</a>
                    {{else}}
                        <a class="nav-link" href="/auth/login">Login</a>
                        <a class="nav-link" href="/auth/register">Register</a>
                    {{/if}}

                    <a class="nav-link" href="/health">Health Check</a>
                </div>
            </div>
        </div>
    </nav>

    <!-- Hero Section -->
    <header class="hero-section py-5 mb-4 text-center text-white">
        <div class="container">
            <h1 class="hero-title mb-2">Your Dashboard</h1>
            <p class="hero-subtitle mb-0">Manage your GameDay Eats experience</p>
        </div>
    </header>

    <!-- DASHBOARD CONTENT -->
    <main class="container my-5">
        <div class="row justify-content-center">
            <div class="col-md-8 col-lg-6">
                <div class="card shadow-sm border-0">
                    <div class="card-body p-4">
                        <h2 class="h5 mb-3">Welcome, {{user.email}} 👋</h2>
                        
                        <p class="text-muted">You are successfully logged in.</p>

                        <hr>

                        <h5 class="mb-3">Quick Actions</h5>
                        <ul class="list-group mb-4">
                            <li class="list-group-item">
                                <a href="/" class="text-decoration-none">🏠 Go to Home</a>
                            </li>
                            <li class="list-group-item">
                                <a href="/api/restaurants" class="text-decoration-none">🍽 View Restaurant API</a>
                            </li>
                            <li class="list-group-item">
                                <a href="/auth/logout" class="text-decoration-none text-danger">🚪 Logout</a>
                            </li>
                        </ul>

                        <p class="small text-muted">
                            This dashboard will expand later with saved restaurants, preferences, and more.
                        </p>
                    </div>
                </div>
            </div>
        </div>
    </main>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
