{{> header}}

<div class="container my-5">
    <div class="row justify-content-center">
        <div class="col-md-10 col-lg-10">
            <div class="card shadow-sm border-0">
                <div class="card-body p-4">
                    <h2 class="h4 mb-3">Welcome, {{user.email}} 👋</h2>
                    
                    {{#if user.firstName}}
                        <p class="text-muted mb-4">Hello, {{user.firstName}} {{user.lastName}}!</p>
                    {{/if}}
                    
                    <!-- Profile Summary -->
                    <div class="row mb-4">
                        <div class="col-md-12">
                            <div class="card border-0 bg-light">
                                <div class="card-body">
                                    <h6 class="card-title text-muted">ACCOUNT INFO</h6>
                                    <p class="mb-1"><strong>Email:</strong> {{user.email}}</p>
                                    {{#if user.firstName}}
                                        <p class="mb-0"><strong>Name:</strong> {{user.firstName}} {{user.lastName}}</p>
                                    {{/if}}
                                    {{#if user.role}}
                                        <p class="mb-0"><strong>Role:</strong> <span class="badge bg-primary">{{user.role}}</span></p>
                                    {{/if}}
                                </div>
                            </div>
                        </div>
                    </div>
                    
                    <hr>
                    
                    <!-- BUSINESS MANAGEMENT SECTION -->
                    <h5 class="mb-3">Business Management</h5>
                    <div class="row g-3 mb-4">
                        <div class="col-md-6">
                            <div class="card h-100 border-0 shadow-sm">
                                <div class="card-body text-center">
                                    <div class="mb-3">
                                        <span class="display-4">🏢</span>
                                    </div>
                                    <h5 class="card-title">My Businesses</h5>
                                    <p class="card-text text-muted small">View and manage your listed businesses</p>
                                    {{#if userBusinesses.length}}
                                        <p class="text-success fw-bold mb-3">{{userBusinesses.length}} business(es) listed</p>
                                    {{else}}
                                        <p class="text-muted mb-3">No businesses listed yet</p>
                                    {{/if}}
                                    <a href="/business/my-businesses" class="btn btn-custom w-100">Manage Businesses</a>
                                </div>
                            </div>
                        </div>
                        
                        <div class="col-md-6">
                            <div class="card h-100 border-0 shadow-sm">
                                <div class="card-body text-center">
                                    <div class="mb-3">
                                        <span class="display-4">➕</span>
                                    </div>
                                    <h5 class="card-title">Add New Business</h5>
                                    <p class="card-text text-muted small">List a new restaurant or venue</p>
                                    <a href="/business/create" class="btn btn-outline-custom w-100">Create Business</a>
                                </div>
                            </div>
                        </div>
                    </div>
                    
                    <!-- Quick Business Actions (if user has businesses) -->
                    {{#if userBusinesses.length}}
                    <div class="card border-0 bg-light mb-4">
                        <div class="card-body">
                            <h6 class="card-title text-muted mb-3">QUICK ACTIONS</h6>
                            <div class="row">
                                {{#each userBusinesses}}
                                <div class="col-md-6 mb-2">
                                    <div class="d-flex justify-content-between align-items-center p-2 bg-white rounded">
                                        <div>
                                            <h6 class="mb-0">{{this.name}}</h6>
                                            <small class="text-muted">{{this.type}} • {{this.city}}</small>
                                        </div>
                                        <div>
                                            <a href="/business/edit/{{this._id}}" class="btn btn-sm btn-outline-primary me-1">Edit</a>
                                            <a href="/business/view/{{this._id}}" class="btn btn-sm btn-custom">View</a>
                                        </div>
                                    </div>
                                </div>
                                {{/each}}
                            </div>
                        </div>
                    </div>
                    {{/if}}
                    
                    <hr>
                    
                    <!-- ACCOUNT MANAGEMENT SECTION -->
                    <h5 class="mb-3">Account Management</h5>
                    <div class="row g-3 mb-4">
                        <div class="col-md-6">
                            <div class="card h-100 border-0 shadow-sm">
                                <div class="card-body text-center">
                                    <div class="mb-3">
                                        <span class="display-4">👤</span>
                                    </div>
                                    <h5 class="card-title">Edit Profile</h5>
                                    <p class="card-text text-muted small">Update your name and email</p>
                                    <a href="/user/profile" class="btn btn-custom w-100">Edit Profile</a>
                                </div>
                            </div>
                        </div>
                        
                        <div class="col-md-6">
                            <div class="card h-100 border-0 shadow-sm">
                                <div class="card-body text-center">
                                    <div class="mb-3">
                                        <span class="display-4">🔒</span>
                                    </div>
                                    <h5 class="card-title">Change Password</h5>
                                    <p class="card-text text-muted small">Update your account password</p>
                                    <a href="/user/change-password" class="btn btn-outline-custom w-100">Change Password</a>
                                </div>
                            </div>
                        </div>
                    </div>
                    
                    <!-- DELETE ACCOUNT BUTTON -->
                    <div class="text-center mt-5">
                        <button type="button" class="btn btn-outline-danger" data-bs-toggle="modal" data-bs-target="#deleteAccountModal">
                            🗑 Delete My Account
                        </button>
                        <p class="text-muted small mt-2">
                            This action cannot be undone. All your data will be permanently deleted.
                        </p>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<!-- Delete Account Modal -->
<div class="modal fade" id="deleteAccountModal" tabindex="-1" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">Delete Account</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
            </div>
            <div class="modal-body">
                <p class="text-danger"><strong>⚠️ Warning: This action cannot be undone!</strong></p>
                <p>Are you sure you want to delete your account? This will:</p>
                <ul>
                    <li>Permanently delete your account</li>
                    <li>Remove all your personal information</li>
                    <li>Delete any saved preferences or history</li>
                </ul>
                <p>To confirm, please type your email address below:</p>
                <input type="email" id="confirmEmail" class="form-control" placeholder="Enter your email">
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
                <button type="button" id="confirmDeleteBtn" class="btn btn-danger" disabled>Delete My Account</button>
            </div>
        </div>
    </div>
</div>

{{> footer}}

<script>
    // Delete account confirmation logic
    document.addEventListener('DOMContentLoaded', function() {
        const confirmEmailInput = document.getElementById('confirmEmail');
        const confirmDeleteBtn = document.getElementById('confirmDeleteBtn');
        const userEmail = "{{user.email}}";
        
        confirmEmailInput.addEventListener('input', function() {
            if (this.value === userEmail) {
                confirmDeleteBtn.disabled = false;
                confirmDeleteBtn.textContent = 'Delete My Account';
            } else {
                confirmDeleteBtn.disabled = true;
                confirmDeleteBtn.textContent = 'Enter correct email';
            }
        });
        
        confirmDeleteBtn.addEventListener('click', function() {
            if (!confirm('Are you absolutely sure? This cannot be undone!')) return;
            
            // Show loading state
            this.disabled = true;
            this.innerHTML = '<span class="spinner-border spinner-border-sm"></span> Deleting...';
            
            // Send DELETE request
            fetch('/user/delete-account', {
                method: 'DELETE',
                headers: {
                    'Content-Type': 'application/json',
                }
            })
            .then(response => {
                if (response.ok) {
                    window.location.href = '/';
                } else {
                    alert('Error deleting account. Please try again.');
                    location.reload();
                }
            })
            .catch(error => {
                console.error('Error:', error);
                alert('Error deleting account. Please try again.');
                location.reload();
            });
        });
    });
</script>